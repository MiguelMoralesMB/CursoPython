import zipfile
import shutil

# mi_zip = zipfile.ZipFile("archivo_comprimido.zip", "w") # Crea un archivo zip vacio en modo escritura
# mi_zip.write("mi_texto_A.txt")
# mi_zip.write("mi_texto_B.txt")
# mi_zip.close()

# zip_abierto = zipfile.ZipFile("archivo_comprimido", "r") # Abre el archivo zip en modo lectura
# zip_abierto.extractall()
# zip_abierto.close()

# carpeta_origen = "C:\\Workspace\\Estudio Programación\\practica_python\\dia-09\\carpeta_superior"

# archivo_destino = "todo_comprimido"

# shutil.make_archive(archivo_destino, "zip", carpeta_origen) # Comprime la carpeta origen en un archivo zip

# shutil.unpack_archive("todo_comprimido.zip", "extraccion_terminada", "zip") # Descomprime el archivo zip en la carpeta de destino